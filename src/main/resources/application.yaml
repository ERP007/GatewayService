server:
  port: 8080
  forward-headers-strategy: framework
  servlet:
    session:
      timeout: ${SESSION_TIMEOUT:2h}
      cookie:
        name: ${SESSION_COOKIE_NAME:GATEWAY_SESSION}
        same-site: ${SESSION_COOKIE_SAME_SITE:none}
        secure: ${SESSION_COOKIE_SECURE:true}

app:
  frontend-base-url: ${FRONTEND_BASE_URL:http://localhost:5173}
  session-invalidation:
    messaging:
      enabled: ${SESSION_INVALIDATION_MESSAGING_ENABLED:true}
      payload-logging-enabled: ${SESSION_INVALIDATION_PAYLOAD_LOGGING_ENABLED:false}
      exchange: ${SESSION_INVALIDATION_EXCHANGE:erp.events}
      queue: ${SESSION_INVALIDATION_QUEUE:gateway.user-session-invalidation.q}
      routing-key: ${SESSION_INVALIDATION_ROUTING_KEY:user.authority.changed.gateway}
      retry-exchange: ${SESSION_INVALIDATION_RETRY_EXCHANGE:gateway.session-invalidation.retry}
      retry-queue: ${SESSION_INVALIDATION_RETRY_QUEUE:gateway.user-session-invalidation.retry.q}
      retry-routing-key: ${SESSION_INVALIDATION_RETRY_ROUTING_KEY:user.authority.changed.gateway.retry}
      retry-delay-ms: ${SESSION_INVALIDATION_RETRY_DELAY_MS:30000}
      max-retry-attempts: ${SESSION_INVALIDATION_MAX_RETRY_ATTEMPTS:5}
      dead-letter-exchange: ${SESSION_INVALIDATION_DLX:gateway.session-invalidation.dlx}
      dead-letter-queue: ${SESSION_INVALIDATION_DLQ:gateway.user-session-invalidation.dlq}
      dead-letter-routing-key: ${SESSION_INVALIDATION_DLQ_ROUTING_KEY:user.authority.changed.gateway.dlq}
      dlq-redrive:
        enabled: ${SESSION_INVALIDATION_DLQ_REDRIVE_ENABLED:true}
        fixed-delay-ms: ${SESSION_INVALIDATION_DLQ_REDRIVE_FIXED_DELAY_MS:60000}
        batch-size: ${SESSION_INVALIDATION_DLQ_REDRIVE_BATCH_SIZE:10}
        max-attempts: ${SESSION_INVALIDATION_DLQ_REDRIVE_MAX_ATTEMPTS:3}
  debug:
    oauth2-token-enabled: ${OAUTH2_TOKEN_DEBUG_ENABLED:false}
    oauth2-token-include-values: ${OAUTH2_TOKEN_DEBUG_INCLUDE_VALUES:false}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:${FRONTEND_BASE_URL:http://localhost:5173}}

spring:
  config:
    import: optional:file:.env[.properties]

  application:
    name: gateway-service

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

  session:
    data:
      redis:
        repository-type: indexed
        namespace: erp:gateway:session
        flush-mode: on_save

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: user-service
              uri: ${USER_SERVICE_URI:http://localhost:8081}
              predicates:
                - Path=/api/users/**

              filters:
                - StripPrefix=1
                - TokenRelay=
  security:
    oauth2:
      client:
        registration:
          keycloak:
            provider: keycloak
            client-id: ${KEYCLOAK_CLIENT_ID:erp-client}
            client-secret: ${KEYCLOAK_CLIENT_SECRET}
            authorization-grant-type: authorization_code
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

        provider:
          keycloak:
            issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:9001/realms/master}
            user-name-attribute: sub
